<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.babyassistant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Helper.AppController"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Controller.StaticPage.About"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".Controller.StaticPage.SosialMedia" />
        <activity android:name=".Controller.Profile.Login" />
        <activity android:name=".Controller.Profile.Register" />
        <activity android:name=".Controller.Profile.Profile" />
        <activity android:name=".Controller.Profile.ActivationAccount" />
        <activity android:name=".Controller.Profile.ForgotPassword" />
        <activity android:name=".Controller.Info.InfoGizi" />
        <activity android:name=".Controller.Info.InfoPenyakit"></activity>
    </application>

</manifest>